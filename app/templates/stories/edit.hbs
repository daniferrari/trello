<h1>Edit </h1>

<div class="form-horizontal">
  <div class="form-group">
    <label class="col-sm-2 control-label">Title</label>
    <div class="col-sm-10">
      {{input type="text" value=model.title class="form-control" placeholder="Please enter the title of the stories" autofocus="autofocus"}}
    </div>
  </div>
  <div class="form-group">
    <label class="col-sm-2 control-label">Due Date</label>
    <div class="col-sm-10">
      {{input type="text" value=model.dueDate class="form-control" placeholder="Please enter the due date of the stories"}}
    </div>
  </div>
  <div class="form-group">
    <label class="col-sm-2 control-label">Tasks</label>
    <!-- <div class="col-sm-10">
      <select onchange={{action "listAuthors" value="target.value"}}>
      {{#each model.authors as |author|}}
        <option value={{author.id}}>{{author.name}}</option>
      {{/each}}
    </select>
    </div> -->
  </div>

  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <button type="submit" class="btn btn-default" {{action 'saveStorie' model}}>Add to stories list</button>
    </div>
  </div>
</div>
